<!DOCTYPE html>
<html>
  <head>
    <title>Test App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <style>
      body { font-family: sans-serif; }
      .form-group, .form-submit {
        margin: 1.5rem 0;
        padding: 1rem;
        background-color: #f0f0f0;
        width: fit-content;
        border-radius: 0.5rem;
      }
      .form-submit { background-color: #e0ffe0; }

      label {
        display: block;
        font-weight: bold;
      }

      .form-errors {
        margin: 0.5rem 0 0 0;
        color: #990000;
        font-size: small;
      }
      input.invalid {
        background-color: #f66;
      }

      .other {
        background-color: #99f;
      }
      .other.invalid {
        background-color: #f9f;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
