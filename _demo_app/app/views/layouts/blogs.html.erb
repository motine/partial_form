<!DOCTYPE html>
<html>
  <head>
    <title>Test App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <style>
      body { font-family: sans-serif; line-height: 1.2; }
      h1, h2 { margin: 3rem 0 1.5rem 0; }
      label { display: block; font-weight: bold; }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
